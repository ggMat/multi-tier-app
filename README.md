# Multi-tier demo app
A tiny Flask app that returns a JSON response — used as the application layer in the portfolio project.

Run locally:
$ python3 -m venv .venv
$ source .venv/bin/activate
$ pip install -r requirements.txt
$ python app.py
